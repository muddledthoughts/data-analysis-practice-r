exam_grades <- c(92, 90, 84, 95, 77, 92, 85)
homework_grades <- c(87, 81, 95, 86, 85, 90, 88)
project_grades <- c(85, 92, 79, 93, 90, 91, 95)

class_names <- c("math", "chemistry", "writing", "art", "history", "music", "physical_education")

#The vectors exam_grades, homework_grades, project_grades, and class_names are provided in the editor.

#Use the function names() to name elements in the vector exam_grades with the class names vector, class_names.

names(exam_grades) <- class_names

#Use the function names() to name elements in the vector homework_grades with the class names vector, class_names.

names(homework_grades) <- class_names

#Use the function names() to name elements in the vector project_grades with the class names vector, class_names.

names(project_grades) <- class_names

